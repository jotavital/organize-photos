This script gets the metadata of a file (video, picture) and renames the file to the "Date Taken" attribute of the metadata.

This is to organize my picture library.

To build:
```pyinstaller --onefile .\main.pyw```
